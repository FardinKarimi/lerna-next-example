{
  "name": "root",
  "devDependencies": {
    "@babel/core": "7.3.4",
    "@babel/preset-env": "7.3.4"
  },
  "scripts": {
    "bootstrap": "npx lerna bootstrap --hoist",
    "build": "npx lerna run build --stream",
    "dev": "npx lerna run dev --parallel --stream",
    "public": "npx lerna run dev --scope=@company/public --stream",
    "buyer": "npx lerna run dev --scope=@company/buyer --stream",
    "seller": "npx lerna run dev --scope=@company/seller --stream",
    "clean": "npx lerna clean",
    "eslint": "npx lerna run eslint --stream --parallel",
    "start": "npx lerna run start --parallel",
    "serve": "npx lerna run start --stream --parallel",
    "version": "npx lerna version"
  }
}
