{
  "name": "root",
  "devDependencies": {
    "@babel/core": "7.5.0",
    "@babel/preset-env": "7.5.0",
    "lerna": "3.15.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "build": "lerna run build --stream",
    "dev": "lerna run dev --parallel --stream",
    "public": "lerna run dev --scope=@company/public --stream",
    "buyer": "lerna run dev --scope=@company/buyer --stream",
    "seller": "lerna run dev --scope=@company/seller --stream",
    "clean": "lerna clean",
    "eslint": "lerna run eslint --stream --parallel",
    "serve": "lerna run start --parallel",
    "version": "lerna version"
  }
}
